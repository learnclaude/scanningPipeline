# CI/CD Environment Configuration
# This file contains environment variables for the CI/CD pipeline

# Docker Registry Configuration
DOCKER_REGISTRY=docker.io
DOCKER_IMAGE=filename-generator
DOCKER_USERNAME=your-dockerhub-username
# DOCKER_PASSWORD should be set as a secret

# Git Configuration
GIT_REPO_URL=https://github.com/your-org/filename-generator.git
GIT_BRANCH=test

# Environment-specific tags
STAGING_TAG=test-latest
PRODUCTION_TAG=prod

# Webhook URLs for notifications and approvals
STAGING_WEBHOOK_URL=https://your-webhook-service.com/staging/deploy
PROD_DEPLOYMENT_WEBHOOK=https://your-webhook-service.com/prod/approval
SLACK_WEBHOOK=https://hooks.slack.com/services/YOUR/SLACK/WEBHOOK

# API URLs for different environments
STAGING_API_URL=http://localhost:18093/api
PRODUCTION_API_URL=https://your-production-domain.com/api

# Monitoring and logging
LOG_LEVEL=info
ENABLE_MONITORING=true
SENTRY_DSN=your-sentry-dsn

# Build configuration
BUILD_ID=${GITHUB_RUN_NUMBER:-local}
BUILD_DATE=${GITHUB_EVENT_HEAD_COMMIT_TIMESTAMP}
VCS_REF=${GITHUB_SHA:-local}